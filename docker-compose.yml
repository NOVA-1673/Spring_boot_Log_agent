services:
  postgres:
    image: postgres:16
    container_name: obs-postgres
    environment:
      POSTGRES_DB: observability
      POSTGRES_USER: obs
      POSTGRES_PASSWORD: obs_pw
    ports:
      - "5432:5432"
    volumes:
      - obs_pgdata:/var/lib/postgresql/data

volumes:
  obs_pgdata:
